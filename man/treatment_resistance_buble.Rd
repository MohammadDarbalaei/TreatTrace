% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Tr_resistance_bubble.R
\name{treatment_resistance_buble}
\alias{treatment_resistance_buble}
\title{Treatment Resistance Bubble Plot}
\usage{
treatment_resistance_buble(
  model,
  cdf_data,
  mean,
  q_up = 0.975,
  q_lo = 0.025,
  min_w = 1,
  max_w = 8,
  min_b = 1,
  max_b = 8
)
}
\arguments{
\item{model}{A list containing model parameters, including `condition_count`.}

\item{cdf_data}{A data frame with columns: `cell` (cell lines), `CDF` (cumulative distribution function values),
and `treat` (treatment groups).}

\item{mean}{A numeric vector of mean values corresponding to the data.}

\item{q_up}{Upper quantile threshold for optional filtering (default = 0.975).}

\item{q_lo}{Lower quantile threshold for optional filtering (default = 0.025).}

\item{min_w}{Minimum bubble size for within-treatment heatmap (default = 1).}

\item{max_w}{Maximum bubble size for within-treatment heatmap (default = 8).}

\item{min_b}{Minimum bubble size for treatment-level heatmap (default = 1).}

\item{max_b}{Maximum bubble size for treatment-level heatmap (default = 8).}
}
\value{
A list containing two ggplot objects:
\itemize{
  \item `bubel_heatmap_w`: Heatmap showing directional differences between cell lines within treatments.
  \item `bubel_heatmap_b`: Heatmap summarizing treatment-level comparisons.
}
}
\description{
This function calculates directional differences and mean differences within treatments,
and generates bubble heatmaps for visualizing resistance patterns.
}
\examples{
# Example usage:
model <- list(condition_count = c("Treatment1", "Treatment2"))
cdf_data <- data.frame(cell = c("A", "B"), CDF = c(0.1, 0.9), treat = c("Treatment1", "Treatment1"))
mean <- c(1.5, 2.5)
treatment_resistance_buble(model, cdf_data, mean)
}
